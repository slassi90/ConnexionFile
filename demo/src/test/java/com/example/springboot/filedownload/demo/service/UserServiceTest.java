/*
package com.example.springboot.filedownload.demo.service;

import com.example.springboot.filedownload.demo.DemoApplication;
import com.example.springboot.filedownload.demo.model.User;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.web.multipart.MultipartFile;

import static org.junit.jupiter.api.Assertions.*;
*/
//@RunWith(SpringRunner.class)
//@SpringBootTest(classes = DemoApplication.class)
class UserServiceTest {
  //  @Autowired
    //private UserService userService;
/*
@Test
public void ShouldsaveuserSuccessfly(){}
User saveuser = User.builder()
        .firstname("Achraf")
        .lastname("harouach")
        .email("sfd@gmail.com")
        .avatar("lcfù")
        .city("sale")
        .company("fff")
        .country("morrocco")
        .jobposition("developer")
        .birthDate("28/09/1990")
        .username("username")
        .password("ldkzfmdsk")
        .mobile("08866687")
        .role("admin")
        .build();
User saveduser = userService.saveUsers(saveuser);
assertEquals(saveduser.getcontent(),saveuser.getcontent());

*/
}


